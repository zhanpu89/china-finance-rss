# 对抗性盲审报告

**模式**: P8 盲审（零上下文、对抗性假设）  
**需求原文**: 删除北向接口（/market/northbound 与 /market/northbound/history），因北向数据已不更新、无实际价值、减少维护成本。修改主 agent 权限收紧（可操作 git，禁止一切修改与编辑权限）。  
**改动范围**: 8 files changed, 25 insertions(+), 221 deletions(-)  
**审查方法**: 逐文件审查代码 + grep 全库残余引用 + 逆向假设

---

## 评审结论

⚠️ **可交付，但有 1 项 P2 遗留问题需关注。**

北向删除彻底、无残余引用；margin 功能未被误伤；文档一致；权限结构合法。总体变更质量高，无 P0/P1 阻断项。

---

## 详细发现

### 【P2】config.py:158: `_china_trading_ttl()` 为死代码

- **现象**: `config.py` 第 158 行定义了 `_china_trading_ttl()` 函数（标注 "Backward-compat"），但全库 grep 确认**零调用方**——无任何 `.py` 文件 import 或引用该函数。
- **证据链**: 当执行 `grep -rn "_china_trading_ttl" --include="*.py"` → 仅命中 `config.py:158`（定义处），无其他匹配 → 该函数在当前代码库中为不可达代码。
- **风险**: 低。不导致运行时错误，但增加维护认知负担。标注 "Backward-compat" 暗示曾有外部调用方，现已移除。
- **建议**: 删除该函数。或保留但添加 `# TODO: remove if no external callers` 注释。

### 无发现项（经审查确认安全）

#### 1. 北向删除完整性 ✅

- **grep 全库验证**: `northbound|NORTHBOUND|北向` 在 `*.py` 文件中 → **零匹配**。
- **市场引用**: `fetch_northbound`, `handle_northbound`, `_transform_northbound` → **零匹配**。
- **配置引用**: `_NORTHBOUND_URL`, `_NORTHBOUND_HEADERS`, `_NORTHBOUND_FEED` → **零匹配**。
- **imports**: `server.py` 第 63-65 行 `from market_api import (handle_margin,)` — 仅导入 margin，无北向残留。
- **文档**: `API.md` / `README.md` 中无北向端点。唯一残留在 `doc/review/系统_代码评审报告_001.md` 第 27 行（历史评审文档，非源码/契约文档，排除）。

#### 2. Margin 功能完整性 ✅

- **market_api.py**: 第 1-80 行，包含 `fetch_margin`、`_transform_margin`、`handle_margin` 三个函数，全部完整。
- **config.py**: 第 87-90 行 `_MARGIN_URL`、`_MARGIN_HEADERS`、`_MARGIN_CACHE_TTL` 完整保留。
- **server.py 路由**: 第 599-603 行 `/market/margin` 路由 → `handle_margin(market)` 调用正确。
- **健康检查**: `build_health_payload` 第 492-495 行包含 `/market/margin` 端点。
- **首页列表**: `json_apis` 第 761 行包含 `/market/margin` 条目。
- **启动日志**: 第 953 行 `log.info` 包含 margin 端点。
- **测试**: `MarketApiTests` 4 个测试 + 1 个健康断言（第 242-245 行）全部完整。

#### 3. 语法与结构完整性 ✅

- **server.py**: 无未使用 import。所有被 import 的符号（`handle_margin`、`_trading_tiers` 等）均在代码中使用。路由表 5 条 RSS + 非 ROUTES 中的 JSON 端点（finance/market, finance/timeline, quotation/market, market/timeline, cls/hotplate, cls/plate, ths/longhu, market/margin）结构完整。
- **market_api.py**: import 仅 `_MARGIN_URL`, `_MARGIN_HEADERS`, `_MARGIN_CACHE_TTL` from config + `fetch_json` from cache + `json`。无残留无用 import。
- **tests/test_server.py**: 所有 import 符号均在测试代码中使用。`_transform_margin` 和 `handle_margin` 被 `MarketApiTests` 引用。`ROUTES`, `handle_cls_hotplate`, `handle_cls_plate`, `build_health_payload` 被对应测试引用。

#### 4. 文档一致性 ✅

- **API.md 目录**: 4 节（RSS / 个股 / 市场 / 工具），无北向章节残留。
- **API.md 内容**: 第 3 节包含 7 个市场端点（含 margin，不含 northbound），字段描述与代码实现一致。
- **README.md**: RSS 表 5 项、Stock API 表 6 项、Market API 表 7 项，均无北向。
- **doc/detailed/**: 目录为空（无详设文档），跳过契约核对。

#### 5. opencode.json 权限配置 ✅

- **JSON 合法性**: 结构完整，所有引号闭合、嵌套正确。
- **pipeline-orchestrator bash 白名单**: 10 条 git 命令（status/log/diff/fetch/pull/push/add/commit/branch/checkout/stash/remote）+ `*:deny` 兜底。符合"可操作 git，禁止一切修改与编辑权限"需求。
- **edit/write**: 均为 `"deny"`。主 agent 无文件修改能力。
- **只读工具**: `read/glob/grep/task/skill/question` 均为 `"allow"`。`webfetch: "ask"`。
- **逆向假设**: 如果 pipeline-orchestrator 需要读取 skills/rules 文件（SKILL.md 指令要求读取 `.opencode/skills/*/SKILL.md`），`read: "allow"` 可满足。✅ 无功能阻断。

---

## 逆向审查

| 假设 | 检查结果 |
|------|---------|
| 恶意输入传入 `/market/margin?market='; DROP TABLE` | `handle_margin` 仅拼接 URL 路径，不执行 SQL。✅ 无注入风险 |
| 删除北向后 `import market_api` 是否仍成功 | `market_api.py` 仅导出 margin 函数，import 语句已更新。✅ 无 ImportError |
| `_trading_tiers()` 的 L4 注释 "f10, margin" 是否与实际一致 | 当前 L4 覆盖端点确实为 f10 和 margin。✅ 一致 |
| 权限收紧是否导致 pipeline-orchestrator 无法执行 `skill` 工具 | `skill: "allow"` 已显式声明。✅ 无阻断 |

---

## 漂移检测

✅ **无漂移**。北向接口删除为功能移除，非功能新增。所有受影响位置（代码/路由/测试/文档/首页/日志）已同步清理。无残留引用。无文档-代码不一致。
