# PRD 终审报告 — 高效 / 准确 / 稳定 三指标体检优化（v0.3）

- **报告编号**：REV-REQ-20260915-003（终审 / 三审）
- **被评文档**：`doc/prd/perf-stability-optimization.md`（PRD-2026-P6C-01，v0.3 待三审，30 条 AC）
- **前次报告**：`..._需求评审_专家版.md`（v0.1，4 P1 + 8 P2）、`..._需求评审复审_专家版.md`（v0.2，P1-3 未闭环 + 5 P2）
- **评审模式**：需求终审——**聚焦上轮未闭环项与新修项，不全文重审**；前轮已闭环的 P1-1/2/4 仅做回归确认
- **只读核对依据**：server.py（路由表 / `_handle_stock_batch` / health / index）、stock_api.py（`fetch_cls_f10` / `handle_cls_f10` / `fetch_cls_basic_info` / `_handle_cached_batch`）、config.py（`_MAX_BATCH_SIZE`）、cache.py
- **评审日期**：2026-09-15
- **结论**：**✅ 通过（可签收）** — 无 P0，P1 = 0

---

## 一、上轮遗留 / 本轮新修项逐项核验

| ID | 修订方声称 | 只读核验结果 | 判定 |
|----|-----------|-------------|------|
| **P1-3①** `/stock/f10` 移出 A 类 | A 类收窄为 4 面板端点、f10 单列 A′（逐码 `null` + `_errors[code]=cdp_unavailable`），明确「不得断言顶层 error 客体」 | AC-S4 第 148 行确列 A 类 **4 条**；第 150 行 A′ 断言与 AC-S6 / AC-A10 值域一致。**代码核对**：`server.py:574` `/stock/f10` → `_handle_stock_batch(parsed, handle_cls_f10)`；`handle_cls_f10`（stock_api.py:514）→ `_handle_cached_batch`（stock_api.py:182）返回扁平 `{code: data|None}`；`fetch_cls_f10`（stock_api.py:485）Chrome 停时返 None → 响应 `{"sh600519": null}`。归类与断言与实现吻合 | ✅ 闭环 |
| **P1-3②** `/stock/basic_info` 改判 B 类 | C 类改 B 类，Then 改「无 CDP 时仍返回完整数据、`sector_name` 不缺」；C 类空集 | AC-S4 第 152 行 B 类含 basic_info，第 154 行 C=0。**代码核对**：`fetch_cls_basic_info`（stock_api.py:628-669）Phase1 REST basic_info、Phase2 REST stock detail 取 `sector_name`；模块头注第 4 行「basic_info now sources sector from REST only」（stock_api.py:4）。与 §7.2 漂移登记一致，测试口径与实现不再矛盾 | ✅ 闭环 |
| **N1** AC-A7 映射 | §8 新增「批量与池正确性组」收纳 A6/A7/A9/A10；§9 覆盖率 30/30 | §8 第 291 行确含 A6,A7,A9,A10；错误语义组收敛为 A5/A8（第 290 行）。§9 第 298 行声明 30/30 | ✅ 闭环 |
| **N2** §9.1 重构为双摘要 | 不再声称「P2 全部已顺手修订」 | §9.1 分「v0.3」「v0.2」两段摘要；第 317 行自述「v0.2 已修 5 项 + 本轮补修剩余 3 项」；第 336 行明确 P1/P2 全闭环、仅余 §5#5 + §7.2 三条契约裁决（非本文档可闭环） | ✅ 闭环 |
| **N3** §7.2 补登 f10 漂移 | health `configured` + 首页 CDP=False 与 `_navigate_f10` 实现矛盾，归 A′ | §7.2 第 278 行确登记该条。**代码核对**：health `server.py:484-487` f10 `status='configured'` ✓；index `server.py:758` `('/stock/f10', ..., False)` ✓；实现 `_navigate_f10`（stock_api.py:448）走 CDP 导航 ✓ | ✅ 闭环 |
| **N4** 响应包裹定义 | 元信息入下划线保留键 `_errors`/`_truncated`/`_dropped_count`，不用 envelope | §7.1 第 255-263 行新增包裹定义表 + 保留键契约，明确「不采用 envelope（避免改义）」；AC-A9/A10/S6 断言同步改名 | ✅ 闭环 |
| **N5** AC-A9 钉死 | 无截断时两键均不存在 | 第 125 行钉死单一口径：≤50 码两键均不存在（不得 `false`/`0`）；>50 码二者必然同现 | ✅ 闭环 |
| **P2-2** AC-A5 情形×端点矩阵 | 五类情形 × 端点类别矩阵 + 错误体/值域边界口径 | 第 108-118 行：5×端点类别矩阵逐格给出期望（400/404/503/error 客体/逐码 null+`_errors`）+「跨类别即判失配」边界口径 | ✅ 闭环 |
| **P2-3** AC-S3 双注入模式 | 模式 A 秒拒（P95≤1s）/ 模式 B 黑洞（阻塞≤15s），不得合并 | 第 141-145 行两种模式各自单独跑一轮、单独断言；§8 第 292 行「黑名单」已改「故障注入」 | ✅ 闭环 |
| **P2-7** EFF-3 加现状 vs 目标列 | 逐行区分现状与目标 | 第 77-85 行表含「现状」「目标（上限口径）」「超限行为」三列，inflight/Feed/URL 缓存等逐行区分 | ✅ 闭环 |

**8 + 2 项全部真闭环，无一项为「口头声称」。**

---

## 二、值域一致性抽查（残留旧名 / 矛盾扫描）

对全文 `errors` / `truncated` / `dropped_count` / `null` / `_errors` / `_truncated` / `_dropped_count` 做全量扫描（25 处命中）：

- **规范性断言处**（AC-A9/A10、AC-S6、AC-S4 A′、AC-A5 矩阵、§7.1 表、§9 不变量）**一律使用下划线前缀** `_errors` / `_truncated` / `_dropped_count`，命名空间与股票代码（无下划线前缀）隔离口径统一。✅
- **仅剩的无下划线旧名**出现在 §9.1 修订历史行（第 319-320 行 N4/N5 条目，描述「原 `errors`/`truncated` 无下划线」的**历史事实**）——属有意保留的变更记录，非残留断言，不构成矛盾。✅
- **枚举三值** `upstream_timeout` / `upstream_error` / `cdp_unavailable` 在 AC-A5、AC-A10、AC-S6、§7.1 **四处完全一致**，无自由文本、无第四值。✅
- **`null` 语义**：AC-A10 / AC-S6 均声明「保留 API.md 既有 null = 无数据含义，不改写值域」，§7 只增不改一致；判别式「null ∧ ∈`_errors` ⇒ 失败 / null ∧ ∉`_errors` ⇒ 无数据」两处表述一致。✅
- **`_truncated` 语义**：AC-A9（第 125 行）与 §7.1 表（第 260 行）、§7.1 待同步表（第 267 行）三处口径一致（≤50 不存在 / >50 同现且 `_dropped_count = 请求码数 − 50`）。✅

**结论：无旧名残留、无跨节矛盾。**

---

## 三、AC 计数核对

- **定义清单**（grep `^- **AC-[EAS][0-9]+`）：**30 条** — E1-E9（9）/ A1-A10（10）/ S1-S11（11），连续无缺号、无重复。✅
- **§8 映射**逐条清点：E1-E3（延迟）；E4-E9（吞吐与资源，E5/E7 另入帧正确性、E6 另入缓存一致性）；A1/A2（帧）、A3/A4（缓存）、A5/A8（错误语义）、A6/A7/A9/A10（批量与池）；S1/S2/S6/S7（竞态与超时）、S3/S4（降级）、S5/S9（吞吐）、S8/S10/S11（可观测）。**30/30 全部落组**，无漏映射。✅
- **§9 声明**（第 298 行）「AC 总数 30」「30/30 全部落组」与 §8 实测**一致**。✅
- **§9 需求项覆盖**（第 299 行）：M 7 项（R1/R2/R5/R12/R14/R16/R18）、S 8 项（R4/R6/R8/R9/R10/R13/R15/R19）逐条映射均可回指有效 AC，与 §4 优先级标注数量吻合。✅
- **§7.2 漂移条数**：第 336 行称「三条」，表内 `/stock/data`、`/stock/basic_info`、`/stock/f10` 恰为 3 条。✅

---

## 四、回归检查（前轮已闭环 P1-1/2/4 是否被本轮改动破坏）

| 项 | 回归核验 | 结果 |
|----|---------|------|
| **P1-1** R14 值域 + §7 只增不改 | AC-S6（第 137 行）降级值 = `null` ∧ ∈`_errors`；AC-A10（第 129 行）失败/非法/无数据三分类仍在；§7 第 251 行「🟠 STABLE、只增不改、保留 null 原语义」未变；本轮仅把字段名加下划线前缀，语义未动 | ✅ 未破坏 |
| **P1-2** AC-A3 白盒断言 | 第 103 行同 handler（`handle_cls_basic_infos`）/ 同池（`_basic_info_pool`）/ 同 `fetch_json` 入口（`fetch_cls_basic_info`）+ 60 次采样 diff 率=0 完整保留；代码核对 stock_api.py:672 调用链成立 | ✅ 未破坏 |
| **P1-4** §9 逐项映射 + R6/S7 | 第 299 行逐项映射清单完整；R4→A9 / R15→E9 / R6→S7 仍在；AC-S7「现状 31s → 目标 5s」保留 | ✅ 未破坏 |

其余前轮已修项抽查：AC-E7 ≤256MB 临时上限（第 88 行）✅、AC-S11 断线重连（第 171 行）✅、AC-A1 时钟回拨例外（第 98 行）✅、端锁定 🟠 STABLE 于 §7/§9 两处一致（第 251 / 304 / 306 行）✅。

---

## 五、结论

> **✅ 通过（可签收）。** 无 P0，P1 = 0。
>
> - 上轮唯一未闭环项 **P1-3 两处均已按实现修正**并只读核对代码证实（f10 批量扁平映射 / basic_info 两阶段 REST），无自相矛盾。
> - 5 项新 P2（N1-N5）+ 3 项遗留 P2（P2-2/3/7）**全部真闭环**，非口头声称。
> - 值域命名全域一致（仅修订历史保留旧名，属变更记录）；AC 计数 30/30、映射 30/30、§9 声明与实测一致。
> - 前轮已闭环 P1-1/2/4 **未被本轮改动破坏**。

**PRD v0.3 可进入 P1c 签收。**

> 非阻断遗留（不影响下游编码，签字后由编排层跟进，不构成本轮放行条件）：
> 1. §5 待确认项 #5（交易时段是否跳过 CDP 守护重启，倾向「跳过」）——显式开放决策，非接口/数据模型/错误码级必须决断点。
> 2. §7.2 三条契约漂移（`/stock/data`、`/stock/basic_info`、`/stock/f10` 的 CDP 标注）待编排层裁决；AC-S4 已按实现修正，不阻塞 tester。
> 3. 微瑕（P3 级，吹毛求疵，可忽略）：AC-A9 的 `_dropped_count = 请求码数 − 50` 在「请求含重复码」时「请求码数」取原始条数还是去重后条数未明示；tester 用 70 个**互异**码即可无歧义验证，故不阻断。

- **变更记录**：2026-09-15 终审（v0.3，核验 P1-3 两处 + 8 项 P2 闭环 + 值域/计数/回归抽查）。
