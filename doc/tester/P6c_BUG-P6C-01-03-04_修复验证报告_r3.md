# P6c 两批修复验证报告（fix1: stream/stock_api/config · fix2: server/metrics/config）

- 日期：2026-09-16 · 验证：tester · **只读验证**（未改代码/配置/未停容器）
- 部署：`docker compose up -d --build` 重建镜像 → 容器 `running`，`RestartCount=0`，日志 0 处 Traceback
- 冒烟：healthz=200 / finance/market=200 (5.9ms) / stock/data=200 (384ms)

## 1. 编译 + 单元测试

- `py_compile china_finance_rss/*.py tests/*.py` → **exit=0**
- `unittest discover -s tests` → **Ran 222, FAILED (failures=1)**（改前基线 206 通过）

唯一失败：`test_data_layer.BasicInfoSectorCacheTests.test_expired_entry_is_refetched:334`
`AssertionError: None != '食品饮料行业'`。**判定为用例自身矛盾（P2），非产品缺陷**：
`_basic_sector_get` 是「读时惰性删除」——`get(now=ttl+1)` 已把条目删掉，随后
`get(now=ttl-1)` 必然为 None。实证：先 `get(now=ttl-1)` → 返回值正确；先 `get(now=ttl+1)` → 缓存被删空。

## 2. 指标零值发布（BUG-P6C-04）✅ 闭环

`/healthz?check=0` 的 `metrics` 中：`negative_cache_size=0`、`stream_frame_dropped_total=0`、
`stream_slow_client_total=0`、`stream_queue_bytes=0`、`upstream_fail_total` 均为显式 0 值，不再缺失。

## 3. backlog（BUG-P6C-03）✅ 闭环

100 并发 connect 到 8053：`n=100 max=0.0006s p99=0.0005s >1s=0`（修复前有 1.006s SYN 重传样本）。

## 4. SSE 三档节拍实测（tick=8s，预算 0.8×tick=6.4s）

| 档 | 帧数/窗口 | 帧间隔序列(s) | min/max/mean(s) | <1s 连发 | 每帧 items | tick_ms 实测值 | lag | 超预算轮 |
|---|---|---|---|---|---|---|---|---|
| 10 码×1 字段 | 8 / 62s | 2.001, 17.384, 2.002, 12.724, 3.275, 12.602, 3.399 | 2.001/17.384/**7.627** | **0** | 10,10,10,10,10,10,10,10 | 0.02, 0.55, 0.59, 1.28, 4602, 4725, **9385**, **10410** | 1(C1) | 2/8 |
| 50 码×3 字段 | 5 / 62s | 15.257, 12.067, 12.100, 7.302 | 7.302/15.257/**11.681** | **0** | 14,23,35,48,46 | 0.68, **10064**, **10066**, **10099**, **13257** | 1→5(C2) | 4/5 |
| 200 码×3 字段 | 7 / 66s | 8.728, 7.987, 7.991, 9.201, 11.653, 12.472 | 7.987/12.472/**9.672** | **0** | 50,33,42,58,48,47,40 | 4910,5304,5617,5628,5640,6820,**9652**,**10472** | 5→17(C2) | 2/8 |

- 会话累计：`stream_tick_degraded_total=10`、`stream_tick_slip_total=9`、`stream_frame_peak_bytes=1,108,340`、`dropped=0`、`slow=0`。
- **单 tick ≤6.4s：未达标** —— 10×1 有 2/8 轮 9.4s/10.4s；50×3 每轮 10.0–13.3s（含 `>tick` 的 slip）。
- **连发 <1s：已消失** —— 三档最小帧间隔 2.001s，恰好等于下限 `0.25×tick`。
- **10 码小码组节拍 ≈8s：未成立** —— 均值 7.63s 但呈双峰：超预算轮后 `sleep=2s` → 2.0–3.4s 背靠背帧，随后 12.7–17.4s 停顿。
- **大组每码刷新周期** —— 200×3：lag=17，tick 墙钟均值 9.67s ⇒ 实测 ≈17×9.67≈**164s/码**（设计 17×8=136s）。
- 运行时证据：`upstream_fetch_total{quote}=328` / ≈265 个不同码 ⇒ ≈1.24 次/码（无 sector 缓存应为 2）→ **basic_info sector 缓存生效**。

## 5. 缺陷清单

| ID | 等级 | 结论 |
|---|---|---|
| BUG-P6C-05 | P2 | `test_expired_entry_is_refetched` 断言自相矛盾（惰性删除前后互斥），修复轮未跑绿即部署 |
| BUG-P6C-06 | P1 | tick 预算仍未达标：`_PER_FETCH_EST=1.0` 仍偏乐观（实测冷路径 ≈2.2s/worker-call → 51 fetches 实际 ≈14s > 6.4s）。10×1 与 50×3 持续 degraded/slip |
| BUG-P6C-07 | P2（观察） | 反连发下限把「超预算轮」变成 2s 背靠背的重复帧 + 12–17s 停顿，节拍双峰，未达成「steady cadence」设计意图 |
| BUG-P6C-08 | P2（观察） | 订阅池清空后 `stream_refresh_lag_ticks` 仍滞留 17（gauge 不归零） |

## 6. 结论

- **BUG-P6C-03（backlog）✅ 闭环** · **BUG-P6C-04（零值指标）✅ 闭环**
- **BUG-P6C-01 ⚠️ 部分闭环**：连发<1s 消失、C1/C2 容量计算生效（lag=1/5/17 与设计一致）、sector 缓存生效；
  但 **tick 预算仍未达标（BUG-P6C-06）**，节拍呈 2s 背靠背 + 12–17s 停顿的双峰形态。
- **无功能性回归**：221/222 测试通过、部署冒烟全 200、`dropped=0`、无 Traceback、RestartCount=0。

## 7. 清理

三个订阅组（10/50/200 码）已全部 `DELETE` → 日志 3 次 `created` / 3 次 `destroyed`，无残留连接
（`stream_frame_distinct=0`、`queue_bytes=0`），后台客户端 socket 全部关闭。
残留仅内存态且随 TTL 过期：`code_cooldown_list`(18 条 fundflow)、`upstream_fail_total{upstream_error}=87`
（本测试使用的合成码段 600000–600199/601000–601099 中部分码不可取），未落盘、未改仓库。


